using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OrientDownwards : MonoBehaviour
{
    // Update is called once per frame
    void Update()
    {
        transform.up = Vector3.up;
    }
}
